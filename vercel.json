{
  "version": 2,
  "installCommand": "NODE_ENV=development pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter web build && cp -r apps/web/.next .next && cp -r apps/web/public public",
  "framework": "nextjs"
}
