{
  "version": 2,
  "installCommand": "NODE_ENV=development pnpm --dir ../.. install --frozen-lockfile",
  "buildCommand": "echo 'skip'",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ]
}
